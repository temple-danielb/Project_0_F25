
//I'm just defining the header now for random.h to use for random.c when it uses rand_string
#ifndef RANDOM_H   
#define RANDOM_H
#include <stdlib.h>  

// The rand_string function prototype
void rand_string(char *s, size_t size);

#endif 

